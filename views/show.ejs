<!DOCTYPE html>
<html lang="en">
    <%- include("partials/head.ejs") %> 
    <body>
        <a href="/budgtr/" style="position: absolute; top: 0; left: 0;"><button style="font-size: 25px;">🏠</button></a>
        <div class="container" style="margin-top: 40px"></div>
        <h1><%=expenditure.name%></h1>
        <% for(key in expenditure){ %> 
            <table>
                <tr>
                    <td style="font-weight: bold;"><%= key%>: </td> <td><%= expenditure[key]%> </td>
                </tr>
            </table>
            <% } %> 
    </body>
</html>